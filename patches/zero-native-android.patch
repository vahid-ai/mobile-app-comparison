diff --git a/build.zig b/build.zig
index 705703f..f51a24b 100644
--- a/build.zig
+++ b/build.zig
@@ -131,6 +131,8 @@ pub fn build(b: *std.Build) void {
         .name = "zero-native",
         .root_module = desktop_mod,
     });
+    // Required when linking libzero-native.a into Android JNI shared libraries.
+    embed_lib.root_module.pic = true;
     b.installArtifact(embed_lib);
 
     const automation_protocol_mod = module(b, target, optimize, "src/automation/protocol.zig");
diff --git a/examples/android/app/build.gradle b/examples/android/app/build.gradle
index 254ff94..b881b9c 100644
--- a/examples/android/app/build.gradle
+++ b/examples/android/app/build.gradle
@@ -7,6 +7,15 @@ android {
     namespace "dev.zero_native.examples.android"
     compileSdk 35
 
+    compileOptions {
+        sourceCompatibility JavaVersion.VERSION_17
+        targetCompatibility JavaVersion.VERSION_17
+    }
+
+    kotlinOptions {
+        jvmTarget = "17"
+    }
+
     defaultConfig {
         applicationId "dev.zero_native.examples.android"
         minSdk 26
@@ -14,6 +23,10 @@ android {
         versionCode 1
         versionName "0.1.0"
 
+        ndk {
+            abiFilters "arm64-v8a"
+        }
+
         externalNativeBuild {
             cmake {
                 arguments "-DANDROID_STL=c++_shared"
diff --git a/examples/android/gradle.properties b/examples/android/gradle.properties
new file mode 100644
index 0000000..c8f0f2a
--- /dev/null
+++ b/examples/android/gradle.properties
@@ -0,0 +1,4 @@
+android.useAndroidX=true
+android.suppressUnsupportedCompileSdk=35
+org.gradle.jvmargs=-Xmx2g
+kotlin.jvm.target.validation.mode=warning
